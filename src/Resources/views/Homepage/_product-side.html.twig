<img src="{{ product.image }}" alt="{{ product.name }}">
<a href="{{ product.url }}">{{ product.name }}</a>
<div>{{ product.description }}</div>
<div>{{ product.price }}</div>
<a href="{{ product.url }}">Detail produktu</a>
